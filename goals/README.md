# goals/ — data-defined formalization goals

Goal files in this directory are JSON in the exact schema `lms.goals.Goal.save`
/ `Goal.load` use (`lms/goals.py:174-205`): top-level `name`, `description`,
`source`, and a `definitions` list of
`{tag, section, name, content, formalized, artifact_ids}`.

`Goal.load(Path("goals/stacks_kernel_track_b.json"))` works today — verified
against the current `lms.goals` module. Statement `content` is the **verbatim
TeX of the tagged Stacks Project statement** (statements only, never proofs),
so every goal entry is source-anchored and auditable against
`https://stacks.math.columbia.edu/tag/<tag>`.

## Files

| File | Statements | Scope |
|------|-----------:|-------|
| `stacks_kernel_track_b.json` | 40 | Shared kernel Phase 1 Track B: fibred categories + descent. Stacks Ch. 4 §32–41 (categories over categories → 2-Yoneda) + Ch. 8 §3 (descent data). Milestones: 02XX (split ⇔ presheaf of categories), 004B (2-Yoneda), 026E (effective descent). Builds directly on the compiled WC-3 corpus (`lean/LMS/Categories/`) |
| `stacks_kernel_track_a.json` | 20 | Shared kernel Phase 1 Track A: spectral sequences. Stacks Ch. 12 §20–25 (basic notions → exact couples → filtered complexes → double complexes). Milestones: 012W (biregular convergence), 0132 (first-quadrant SS). A first curated slice of the track, not its full ~40–50-statement extent |

**Track C (core algebra: linearly topologized rings, I-adic completion, DVR
extensions) is TODO** — sources are Stacks `algebra.tex`/`more-algebra.tex`;
add a curation table to the extractor when it is taken up.

## Provenance / regeneration

Generated by `scripts/goals/extract_stacks_goal.py` from a local clone of the
Stacks Project (untracked; default `references/stacks-project/`, override with
`STACKS_PROJECT_DIR`). Tags come from the clone's `tags/tags` file, never from
memory. To change a track's scope, edit the curation table in the script and
regenerate — do not hand-edit the JSON.

## Registration (one small patch, not yet applied)

The CLI resolves `--goal` through `GOALS_REGISTRY` (`lms/goals.py:1341`), which
holds Python-defined goals only. To make these files reachable, in `get_goal`
(`lms/goals.py:1350`) fall back to the data directory before raising:

```python
path = Path(__file__).resolve().parent.parent / "goals" / f"{name.replace('-', '_')}.json"
if path.exists():
    return Goal.load(path)
```

and extend `Goal.load` to restore the three optional fields it currently drops
(`allowed_imports=data.get("allowed_imports")`,
`forbidden_imports=data.get("forbidden_imports")`,
`preamble=data.get("preamble")` — all backward-compatible with run-saved
`goal.json` files, which lack the keys). That makes the invocation
`--goal stacks-kernel-track-b`. This patch is deliberately **not** in this
branch: `26Q3-HARN-12` is rewiring `lms/` in parallel and this directory must
stay docs/data-only; land it as a follow-up once HARN-12 merges (see the
26Q3-STACKS-01 card).
